(function() {
	angular
		.module('TrendController', ['ui.bootstrap'])
		.controller('trendCtrl', ['$http','$uibModal','$log','$document',function($http,$uibModal, $log, $document) {
			var selt = this;
		}]);
})();

